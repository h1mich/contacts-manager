import CMButton from './CMButton';
import CMCheckbox from './CMCheckbox';
import CMErrorModal from './CMErrorModal';
import CMInput from './CMInput';
import CMLoader from './CMLoader';
import CMModal from './CMModal';

// This construction allows to use the following syntax for importing reusable components:
// import { CMInput, CMErrorModal } from './common'
export {
  CMButton,
  CMCheckbox,
  CMErrorModal,
  CMInput,
  CMLoader,
  CMModal
};